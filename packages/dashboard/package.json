{
  "name": "@bsr-method/dashboard",
  "version": "0.1.0",
  "description": "Local dashboard for BSR Method",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "bsr-dashboard": "dist/server.js"
  },
  "scripts": {
    "build": "npx tsup",
    "dev": "npx tsup --watch",
    "start": "node dist/server.js",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/static": "^7.0.0",
    "@fastify/websocket": "^9.0.0",
    "fastify": "^4.25.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.18.1",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
